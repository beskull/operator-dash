// This file was the original single-file starter (salvaged from RTF).
// The app is now a real Vite project — the live entry point is:
//
//   src/App.tsx          — composition + derived layout
//   src/types.ts         — Board / Workspace / Panel / Window / Module model
//   src/data/boards.ts   — mock boards, workspaces, mode layouts
//   src/components/      — AppShell, Panel, WindowFrame, FlattenDock,
//                          FloatingLayer, ControlPanel, BackgroundCanvas,
//                          DebugInspector, ModuleHost
//   src/modules/         — mock app surfaces (ClaudeCode, FluxCanvas, Logs,
//                          StatusCard, Dashboard, Sessions, Chat, Docs,
//                          PatentSearch, Marketing)
//
// Run it:  npm install && npm run dev
export {};
